serviceName: "example-service"

# Service configuration
service:
  type: "ClusterIP"  # Changed from LoadBalancer - use Ingress for external access
  port: 80
  targetPort: 8000
  annotations: {}

# Ingress configuration for AKS Web Application Routing (NGINX-based add-on)
ingress:
  enabled: true
  host: ""  # Leave empty for path-based routing, or set hostname for host-based
  path: ""  # Defaults to /{serviceName}
  pathType: "Prefix"
  requestTimeout: 30
  tls: []
  annotations: {}

# Container image
image:
  repository: "ghcr.io/azure-samples/holiday-peak-hub/example"
  tag: "latest"

replicaCount: 2

# Resource limits and requests
resources:
  limits:
    cpu: "500m"
    memory: "512Mi"
  requests:
    cpu: "250m"
    memory: "256Mi"

# Health probes
probes:
  startup:
    path: /health
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 30
  liveness:
    path: /health
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
  readiness:
    path: /ready
    initialDelaySeconds: 5
    periodSeconds: 10
    timeoutSeconds: 5

# Node selector for targeting specific node pools
# For agents: agentpool: agents
# For crud: agentpool: crud
nodeSelector: {}

# Tolerations for node pool taints
# For agents: { key: "workload", operator: "Equal", value: "agents", effect: "NoSchedule" }
# For crud: { key: "workload", operator: "Equal", value: "crud", effect: "NoSchedule" }
tolerations: []

# KEDA autoscaling configuration
keda:
  enabled: true
  minReplicaCount: 1
  maxReplicaCount: 5
  pollingInterval: 30
  triggers:
    - type: azure-eventhub
      metadata:
        connection: EventHubConnection
        eventHubName: example-hub
        consumerGroup: $Default
        blobContainer: keda-checkpoints
        storageConnection: StorageConnection

# Canary deployment configuration
canary:
  enabled: false  # Disabled by default, enable for progressive rollouts
  replicaCount: 1
  image:
    repository: ""  # Uses main image.repository if empty
    tag: ""  # Uses main image.tag if empty
  ingress:
    enabled: false
    annotations: {}
  headerRouting:
    enabled: false
    headerName: "x-canary"
    headerValue: "true"
