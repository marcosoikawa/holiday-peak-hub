[project]
name = "holiday-peak-scripts"
version = "0.1.0"
description = "Dev helper scripts for Holiday Peak Hub"
requires-python = ">=3.10"
dependencies = [
    "watchfiles>=0.21.0",
    "uvicorn>=0.27.0",
]

[tool.pdm]
venv.in_project = true
